# jalousie
A lightweight native macOS window manager built as a proper .app bundle in Swift. Automatically tiles windows into equal horizontal splits, supports keyboard-driven focus, swapping, and sending windows across spaces — with no scripting additions, no SIP modifications, and no third-party dependencies. Works correctly on macOS Tahoe (26.x).
